const n= 5;
const m = 12;
const fox = { x: n-1, y: m-1 }; // x: sor, y: oszlop
const nyuszi = {x: randint(0,n), y: randint(0,m/2)};
let jatek_fut = false;
let szint = 1;
let ido = 0;

const table = document.querySelector("table")

function randint(a,b){
    return Math.floor(Math.random() * (b-a+1)) + a;
}

function nyuszimuszi{

}
